{
  "name": "ritpraswa-ai-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express server for Ritpraswa AI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
